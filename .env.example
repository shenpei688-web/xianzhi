# 本地开发：复制为 .env，执行 npm run dev:server
# 纯只读行情服务（纳指基金 + 黄金），无需数据库 / 第三方密钥

# API 端口（可选，默认 3001）
# PORT=3001

# 本地站点地址（可选，默认 http://localhost:5173）
# PUBLIC_APP_URL=http://localhost:5173

# 行情缓存（毫秒，可选）
# FUND_CACHE_TTL_MS=30000
